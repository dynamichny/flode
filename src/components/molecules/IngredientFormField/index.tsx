import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import {}

interface Props {}

const IngredientFormField = (props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default IngredientFormField;

const s = StyleSheet.create({});
