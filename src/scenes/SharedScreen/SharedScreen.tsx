import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

interface Props {}

const SharedScreen = (props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default SharedScreen;

const styles = StyleSheet.create({});
